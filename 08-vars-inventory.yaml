- name: Variables inventoty
  hosts: Localhost
  tasks:
  - name: print variable using inventory file
    ansible.builtin.debug:
      msg: "I am going to {{Travel}} for meeting my friend {{Friend}} and will back on {{Day}}"